<link rel="stylesheet" href="css/variables.css">
<footer class="site-footer">
  {%- for privacity in collections.privacity -%}
      <a href="{{ privacity.url }}">{{ privacity.data.title }}</a>
  {%- endfor -%}

  {%- for config in collections.config -%}
      <a href="{{ config.data.github }}" target="_blank" rel="noreferrer noopener" >Github</a>
      <a href="{{ config.data.youtube }}" target="_blank" rel="noreferrer noopener" >Youtube</a>
  {%- endfor -%}
  
</footer>

<style>
.site-footer {
  --logo-color: currentColor;
  padding: 2em 0;
  color: var(--clr-neutral-100);
  background-color: var(--clr-primary-500);
  text-align: center;
}

.site-footer a {
  padding-inline: 3em;
  text-decoration: none;
  color: var(--clr-neutral-100);
}

.site-footer a:hover {
  color: var(--clr-neutral-500);
  border: 10px;
  border-radius: 10px;
  transition: 0.2s;
}
</style>